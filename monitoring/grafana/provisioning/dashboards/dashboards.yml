apiVersion: 1
providers:
  - name: scoring-api
    folder: Prêt à Dépenser
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
